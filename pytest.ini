[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers = 
    slow: marks tests as slow (may take more than 30 seconds)
    network: marks tests that require network access
    auth: marks tests that may require authentication